import 'package:flutter/material.dart';

class BusPaymentScreen extends StatelessWidget {
  const BusPaymentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
