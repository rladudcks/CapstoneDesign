import 'package:flutter/material.dart';

class CourtRegisterScreen extends StatelessWidget {
  const CourtRegisterScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
