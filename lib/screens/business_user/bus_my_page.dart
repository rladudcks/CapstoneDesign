import 'package:flutter/material.dart';

class BusMyPageScreen extends StatelessWidget {
  const BusMyPageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
